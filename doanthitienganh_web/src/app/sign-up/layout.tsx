
export const metadata = {
    title: "Đăng ký tài khoản thi"
  }

export default function SignUpLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return (
      children
    )
  }